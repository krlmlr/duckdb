# Function documentation; source of truth for the sibling functions.json
# (regenerate with: python3 scripts/functions_yaml.py to-json).
- name: row_number
  parameters: ''
  description: The row number in a window partition
  examples:
    - ROW_NUMBER() OVER (PARTITION BY client ORDER BY date)
  categories: ''
  type: window_function
- name: ntile
  parameters: num_buckets
  description: The row bucket in a window partition for a given bucket count
  examples:
    - NTILE(12) OVER (PARTITION BY client ORDER BY month)
  categories: ''
  type: window_function
